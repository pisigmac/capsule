# Future

Worth doing if people actually use this:

- Dedup by content hash
- Embeddings as an optional extra index, not a replacement for FTS
- Git auto-commit of `capsules/`
- A real MCP SDK transport besides newline JSON-RPC

Not planned here: Stripe, seats, Notion import, “world-class glassmorphism.”
